export default function PageTitle({pageTitle}) {
  return (
    <h1 className="bg-clip-text mb-4  text-transparent  font-bold bg-gradient-to-b from-purple-800 to-blue-500 text-7xl text-center mt-10">
      {pageTitle}
    </h1>
  );
}
